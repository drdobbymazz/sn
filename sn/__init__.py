"""sn — a local agent that lives on the phone.

The LLM runs elsewhere (Ollama, reached over Tailscale); the tools run here,
on the device, through termux-api.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
